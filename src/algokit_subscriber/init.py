"""Minimal entrypoint holding version metadata for semantic release."""

version = "0.1.0"


def library_info() -> str:
    """Return a short description of the reference library."""
    return (
        "AlgoKit subscriber reference package for demonstrating shared CI/CD orchestration."
    )
